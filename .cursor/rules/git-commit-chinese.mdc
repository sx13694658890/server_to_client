---
description: Git 提交说明使用简体中文（含 Cursor 提交按钮限制说明）
alwaysApply: true
---

## 重要说明（为何「生成提交信息」仍是英文）

Cursor **源代码管理里 ✨ 生成提交信息** 走的是独立流程，**不会读取** 本项目的 Rules / User Rules。  
这是官方已知的限制；规则只对 **Chat / Agent / 编辑内 AI** 等场景生效。

因此：**单靠添加规则无法让 ✨ 按钮变成中文。** 请用下面任一方式。

---

## 在 Chat / Agent 里生成中文提交说明（推荐）

1. 先 `git add` 暂存改动。  
2. 打开 **Chat**，输入框用 **`@Git`**（或 **`@git`**，以你当前 Cursor 版本显示为准）附上仓库/暂存上下文。  
3. 发送例如：

   > 根据当前**已暂存**的 diff 写一条 Git 提交说明，**必须全部使用简体中文**。  
   > 格式：`feat(范围): 简短标题`，必要时正文用 `- ` 分条。不要英文段落。

4. 把模型输出的标题和正文复制到提交框。

---

## 用「近期提交语言」引导 ✨（辅助，不保证）

新版生成逻辑会参考 **你仓库里最近的 commit message**。  
若你持续用**中文**提交，✨ 生成的语言有可能逐渐贴近中文，但**仍不保证**，且与 Rules 无关。

---

## 编写或生成提交说明时的内容规范（适用于 Chat 生成）

- 使用**简体中文**；标题一行概括改动，正文可选列表。
- 可用约定式前缀 + 中文：`feat`、`fix`、`docs`、`chore`、`refactor` 等，例如 `feat(web): 文档中心增加搜索`。
- 路径、API 名可保留英文；避免整段英文描述业务改动。
